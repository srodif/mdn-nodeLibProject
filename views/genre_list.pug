extends layout

block content
  h1= title

  ul
    each genre in genre_list
      li
        a(href=genre.url) #{genre.name}
        //|  #{genre.url}

    else
      li There are no genre categories in this library.